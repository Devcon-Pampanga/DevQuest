import type { ProfilePageUser } from "@/components/profile/types";

/** Firestore user document shape for the settings screen (aligned with profile). */
export type SettingsPageUser = ProfilePageUser;
